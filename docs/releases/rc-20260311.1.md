# Release Notes: rc-20260311.1

Date: 2026-03-11
Previous Tag: (none)
Commit Range: (none)..rc-20260311.1

## Summary

- Release candidate consolidates production hardening across compensation, RBAC, observability, and release operations.
- Release-readiness automation now includes smoke-test and release-notes tooling for repeatable validation.
- Migration/schema alignment fixes were applied to prevent runtime crashes caused by snake_case/camelCase drift.

## Key Features

- Deployment compensation state tracking with health and metrics signals.
- Caddy validate-and-rollback guard to restore last known-good config on apply failure.
- RBAC with owner/operator/auditor permissions, DB-backed token-hash bindings, startup bootstrap, and deny audit events.
- Route-level RBAC allow/deny integration matrix tests.
- Release workflows: smoke script, release checklist, and release notes generator.

## Migrations

- Applied 13 migrations successfully in fresh database validation run.
- New migration in this RC line: admin role bindings table (`20260311214500_admin_role_bindings`).
- Migration script safety fix: removed duplicate index creation from `20260311150000_deployment_job_counters`.

## Environment Changes

Added:

- `ADMIN_API_OWNER_TOKEN`
- `ADMIN_API_OPERATOR_TOKEN`
- `ADMIN_API_AUDITOR_TOKEN`
- `ADMIN_RBAC_DB_BINDINGS_ENABLED`
- `ADMIN_RBAC_BOOTSTRAP_FROM_ENV`
- `DEPLOY_COMPENSATION_VM_COMPOSE_DOWN_ENABLED`
- `DEPLOY_COMPENSATION_ALERT_FAILURES_24H_HIGH`
- `CADDY_VALIDATE_COMMAND`

Changed:

- Admin auth semantics now support role-scoped permissions with `403` for under-scoped authenticated requests.
- Caddy deploy flow now performs best-effort rollback on validate/reload failure.
- Release process now expects smoke command and generated notes scaffold.

Removed:

- None.

## Operational Notes

- Run migrations before startup: `docker compose run --rm bot npm run prisma:deploy`.
- Run smoke validation against running service: `npm run smoke:test`.
- Generate next notes scaffold with: `npm run release:notes -- <next-tag>`.
- DB mapping alignment in Prisma schema is required for snake_case columns in manually-authored SQL migrations.

## Known Risks

- Smoke suite currently validates key API health/governance paths; it does not yet execute full deployment runner flow.
- RC tag and image tag are created locally; remote push/publish remains a manual release step.

## Verification Evidence

- Build: passed via `docker compose --profile test run --rm bot-test npm run build`.
- Tests: passed (80/80) via rebuilt test image and `npm test`.
- Smoke: passed via `scripts/smoke-test.sh` against running `bot` + `mariadb` stack.

## Changelog (Auto)

- 54cd21b fix: align prisma mappings and migration for release validation
- 23232ca docs: add release notes generator and template
- 705bc4a docs: add release checklist and smoke test command
- 3fee487 docs: operationalize post-v1 roadmap execution
- 2751841 docs: finalize phase 5 architecture decisions
- f2e23b5 test: add rbac route-level allow-deny integration matrix
- de0fbdf feat: add db-backed rbac bindings with startup seeding
- 325d0ae feat: add caddy validate-and-rollback deployment guard
- 0f67ef1 feat: add scoped admin RBAC with role tokens
- 5747110 feat: add deployment compensation state tracking and observability
- 8aeaef7 docs: continue roadmap with phase 5 execution plan
- d0dbb0a chore: commit all current hardening changes
- fa2bcff Phase 15: Add admin API auth-failure observability and alerts
- c2a5297 Phase 14: Add aggregated system alert rollup in health and metrics
- d2f7cbe Phase 13: Add Virtualizor API observability and alert signals
- 6b6a086 Phase 12: Add webhook signature-failure observability and alerts
- f8c4383 Phase 11: Add SSH execution observability and alert flags
- bfe53e3 Phase 10: Add GitHub API circuit-breaker observability and alerts
- 8845aa4 Phase 9: Add secret-encryption alert thresholds and flags
- 76ee92b Phase 8: Harden secret decrypt path with health metrics
- 64e1ac6 Phase 7: Add deployment job failure pattern tracking and counters
- 720ea90 feat: add deployment job retention and cleanup
- cebe1cb Phase 0
- 9c23488 [main] Added Default Configs
- 55f147b Add devcontainer configuration for Docker in Docker
- 070efba Initial commit
